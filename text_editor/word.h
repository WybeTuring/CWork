#ifndef WORD_H
#define WORD_H

/* Reads next word from the input and stores it in word, makes word empty if no word could be found due to end-of-file, truncates the word if it's length excedes the maximum length
*/
void read_word(char *word, int len );

#endif
